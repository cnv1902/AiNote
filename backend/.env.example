APP_ENV=dev
API_PREFIX=/api
HOST=127.0.0.1
PORT=8000

# PostgreSQL (Neon)
DATABASE_URL=

# JWT
JWT_SECRET=change_this_to_a_long_random_secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRES_MINUTES=30
REFRESH_TOKEN_EXPIRES_DAYS=14

# Text search config: simple | english
FTS_CONFIG=simple

# Frontend storage (informational; backend may use for signed URLs later)
SUPABASE_URL=
SUPABASE_ANON_KEY=